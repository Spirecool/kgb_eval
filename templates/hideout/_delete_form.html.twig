<form method="post" action="{{ path('app_hideout_delete', {'id': hideout.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hideout.id) }}">
    <button class="btn">Delete</button>
</form>
